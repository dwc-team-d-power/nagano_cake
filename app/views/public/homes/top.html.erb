<div class="container my-5">
  <div class="row">
    <div class="col-3">
      <h5>ジャンル検索</h5>
      <%= render 'public/items/genre', genres: @genres %>
    </div>
    <div class="col-9">
      <h1 class="my-4 text-center">
        ようこそながのCAKEへ！
        <small class="text-muted d-block">このサイトは、ケーキ販売のECサイトになります。会員でない方も商品の閲覧はできますが、購入には会員登録が必要になります。</small>
      </h1>
      <h3 class="text-center">新着商品</h3>
      <div class="row">
        <% @items.each do |item| %>
          <div class="col-md-4 my-3">
            <div class="card h-100 shadow-sm">
              <% if item.image.attached? %>
                <%= image_tag item.image, class: "card-img-top", size: "100x100" %>
              <% else %>
                <%= image_tag "no_image.jpeg", class: "card-img-top", size: "100x100" %>
              <% end %>
              <div class="card-body">
                <h5 class="card-title"><%= link_to item.name, item_path(item), class: "stretched-link text-decoration-none" %></h5>
                <p class="card-text"><%= item.formatted_price %></p>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <div class="text-right my-4">
    <%= link_to "全ての商品",items_path %>
  </div>
</div>