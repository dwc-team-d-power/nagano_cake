<% if genres.present? %>
  <% genres.each do |genre| %>
    <div class="col-md-4">
      <h2><%= genre.name %></h2>
      <%= link_to genre.name, items_path(genre_id: genre.id) %>
    </div>
  <% end %>
<% else %>
  <p>ジャンルが見つかりませんでした。</p>
<% end %>